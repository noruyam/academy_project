<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper

        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"

        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="nanumDAO">
	
	<insert id="insertBoard" parameterType="board">
		INSERT INTO board(seq, title, writer, content, regDate, cnt)
		VALUES (SQ_board.nextval, #{title}, #{writer}, #{content}, sysdate, 0)
	</insert>
	
	<delete id="deleteBoard" parameterType="board">
		DELETE FROM board
		WHERE seq = #{seq}
	</delete>
	
	<update id="updateBoard" parameterType="board">
		UPDATE board 
		SET title = #{title},
			writer = #{writer},
			content = #{content}
		WHERE seq = #{seq}
	</update>
	
	<select id="getBoard" parameterType="board" resultType="board">
		SELECT *
		FROM board
		WHERE seq = #{seq}
	</select>
	
	<select id="getBoardList"  parameterType="board" resultType="board">
		SELECT *
		FROM board
		order by seq desc
	</select>
	<update id="updateCount" parameterType="board">
	   UPDATE upLoadtemp1
	   SET cnt = cnt + 1
	   WHERE seq = ${seq}
	</update>   
</mapper>
		
